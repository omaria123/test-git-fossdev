[tool.poetry]
name = "discount-service"
version = "0.1.0"
description = "Discount service for calculating order discounts"
authors = ["metsha <ogorodnikovamaria07@gmail.com>"]
license = "MIT"
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.136.1"
uvicorn = {version = "^0.46.0", extras = ["standard"]}
pydantic = "^2.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"