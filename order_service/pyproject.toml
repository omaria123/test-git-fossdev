[project]
name = "order-service"
version = "0.0.1"
description = "application for serving users orders"
authors = [
    {name = "metsha",email = "ogorodnikovamaria07@gmail.com"}
]
license = "MIT"
readme = "README.md"
requires-python = "^3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
